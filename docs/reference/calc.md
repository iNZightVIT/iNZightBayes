# Calculate statistics

Calculate statistics

## Usage

``` r
calc(x, f, ...)
```

## Arguments

- x:

  an `inzposterior` object

- f:

  a function to compute for each parameter

- ...:

  arguments passed to `f`

## Value

a vector or matrix of values
